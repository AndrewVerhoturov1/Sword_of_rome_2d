# V2 Request: Raw Export Redaction Fix — Token Usage Numbers Must Survive

## V2 ID

`V2-20260610-142500`

## Статус

`awaiting_human_push_approval`

## Дата

2026-06-10

## Цель

Получить bounded technical advice от внешнего senior reviewer по правильному архитектурному подходу к исправлению redaction в [`_redact_raw_event()`](../../scripts/codex_token_monitor_server.py). Сейчас usage telemetry-числа (`input_tokens`, `last_token_usage`, `total_token_usage`) уничтожаются redaction-логикой из-за слишком широкого match-а `"token" in key_name`. Нужно сохранить usage-числа для аудита, продолжая редактировать auth-секреты и personal идентификаторы.

## Контекст

### Почему нужен внешний review

Проблема не «как заменить строку» — это тривиально. Вопрос архитектурный: как правильно построить whitelist/blacklist для рекурсивного redactor-а, чтобы:

1. Usage-числа выживали при любых будущих изменениях имён полей в Codex rollout
2. Auth-секреты надёжно редактировались даже при новых именах ключей
3. Подход был понятен при code review и не требовал угадывания

### Что уже пробовали

- Локально найден root cause: строка 763-764 в `_redact_raw_event` — `"token" in k.lower()` матчит `input_tokens`, `cached_input_tokens`, `output_tokens`, `reasoning_output_tokens`, `total_tokens`, `last_token_usage`, `total_token_usage`
- Ясно ЧТО исправить, но неясно КАК архитектурно правильно структурировать

### Связанные issues / bug journal entries

`none`

## Base и Snapshot

### Base commit

```
TBD — будет установлен при push
```

Base branch: `main`

### Snapshot commit

```
TBD — будет установлен при push
```

Review branch: `review/v2/20260610-142500-raw-export-redaction-fix`

### Compare link

```
TBD
```

## Snapshot publication method

- Snapshot method: `same-worktree-with-restore`
- Local workspace preservation: `required`
- Local workspace state: `pending`
- Local restore/verification status: `pending`

## Ключевые ссылки для review

| Файл | Почему важен |
|------|-------------|
| `scripts/codex_token_monitor_server.py` | `_redact_raw_event` (L749-772), `_build_raw_step_export` (L775-855+), `_handle_raw_step_export` (L4620+) |
| `tests/test_codex_token_monitor_server.py` | Тесты raw export |
| `static/codex-token-monitor/app.js` | UI raw export |
| `tests/test_classification_rules.py` | Regression tests |

## Изменённые файлы

| Файл | Статус | Описание |
|------|--------|---------|
| `scripts/codex_token_monitor_server.py` | M | v2.16 fixes + redaction target |
| `tests/test_codex_token_monitor_server.py` | M | Raw export tests |
| `static/codex-token-monitor/app.js` | M | v2.16 UI fixes |
| `static/codex-token-monitor/index.html` | M | cache-bust v=0084 |
| `tests/test_classification_rules.py` | M | v2.16 regression tests |

## Файлы, исключённые из snapshot

| Файл | Причина |
|------|--------|
| `_local/` | local-only |
| `output/` | generated |
| `.ai/reports/` | runtime |
| `.ai/handoffs/` | runtime |

## Вопросы к внешнему senior reviewer

1. **Root cause confirmation:** Подтверди, что строка 763-764 в `_redact_raw_event` — единственное место, где `"token" in key.lower()` ловит usage-поля. Есть ли другие места потери usage-чисел?

2. **Architecture:** Как правильно структурировать whitelist/blacklist для рекурсивного redactor-а? Варианты:
   - (A) Whitelist: поля которые НЕ редактировать (`_tokens`, `_token_usage`, `_token_count`)
   - (B) Blacklist: сузить `"token"` до `"api_token"`, `"auth_token"`, `"bearer"`
   - (C) Type-based: редактировать только строки, числа не трогать
   - (D) Другой подход?

3. **Future-proofing:** Как сделать чтобы новые usage-поля в будущих версиях Codex rollout не ломались? Whitelist-by-pattern?

4. **Event index:** Оборачивать ли каждый raw event в `{event_index, raw: {...}}` или добавить `event_index` внутрь объекта?

5. **Usage source tracking:** Где в human timeline/details лучше всего добавить `Usage raw event #...` и `Usage source`?

## Ожидаемый результат

Bounded technical advice: root cause hypothesis, recommended correction path, risks, implementation notes. Не готовый patch.

## Границы

- Не писать patch без локального repo access
- Не утверждать что видел runtime, tests, shell или git status
- Не менять source split live/archive
- Не запускать новый OTel experiment
- Не писать в `C:/Users/andre/.codex/**`

## Поля политик

- Bug journal: `none`
- Human visual check после реализации: `yes` — проверить raw export, usage-числа должны быть числами
- Human review для V2 request: `required` — human push approval

## История статусов

| Дата | Статус | Комментарий |
|------|--------|------------|
| 2026-06-10 | `draft` | V2 request создан |
