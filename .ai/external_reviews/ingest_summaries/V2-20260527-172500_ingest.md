# V2 Ingest Summary: V3 Phase 2 Contract Pack Review

## V2 ID

`V2-20260527-172500`

## Статус

`implementation_planned`

## Дата ingest

2026-05-27

## Kilo mode использованный для ingest

`kilo-handoff-runner`

## Model

DeepSeek V4 Flash

## Ссылки

- V2 request: `.ai/external_reviews/requests/V2-20260527-172500_request.md`
- V2 response (raw): `.ai/external_reviews/responses/V2-20260527-172500_response.md` (direct paste)

## Что сказал внешний чат — кратко

### Main assessment (своими словами)

Phase 2 Contract Pack directionally strong и в целом acceptable. Контракты создают правильные conceptual layers: request/package/manifest/journal/review/revision/storage/scope/acceptance. V3 не обещает готового runtime. `/v3` не активирован. Это правильный уровень для Phase 2.

Главная проблема — **precision**: термины и пути слегка расходятся между файлами. Сейчас это мелко, но станет реальными багами в Phase 3 (templates) и Phase 7 (importer/scripts).

### Root cause hypothesis

Контракты написаны как coherent conceptual pack, но не нормализованы как machine-facing spec. Это нормально для Phase 2, но так как repo близок к созданию prompts/templates/import scripts, неоднозначности нужно исправить до того, как они перейдут в шаблоны.

### Recommended correction path

Внешний чат выделил 7 issues:

1. **V2 request report URL broken** — request report лежит на `0b30333`, а snapshot на `7607265`.
2. **Central rules URL wrong** — в V2 prompt указан путь `ai/external_chats/external_chat_rules.md`, правильный — `external_chat_rules.md`.
3. **Journal path** — `journal` vs `journals`, pending vs accepted.
4. **"files outside files/" ambiguity** — root control files vs project target files.
5. **expected_files wording** — должно быть project-relative target paths.
6. **Verdict naming inconsistency** — Codex (accept/revision_needed/reject) vs human (accept/revision/reject) + accept_with_notes.
7. **docs_only scope examples** — добавить примеры для contracts/specifications.

### Ключевые риски (от внешнего чата)

- Если не исправить сейчас, Phase 3 templates скопируют неоднозначный язык в prompts.
- Phase 7 importer/validator scripts могут имплементировать неправильные допущения.
- V2 artifacts нестабильны (request report URL сломан) — нужно фиксить независимо от V3.

## Kilo understanding

### Что из ответа подтверждено локально

- Issues 3, 4, 5, 6, 7 — подтверждаю: все неоднозначности действительно есть в контрактах.
- Issue 1 — подтверждаю: request report добавлен commit `0b30333` на review branch, но snapshot `7607265` его не содержит. Это process issue: V2 protocol требует финализировать request metadata до snapshot push, но request был создан POST-push как часть обязательной цепочки.
- Issue 2 — подтверждаю: central rules URL в V2 prompt содержит `.ai/` в пути, правильный URL без `.ai/`.

### Что осталось гипотезой

- Все 7 issues подтверждены локальным чтением файлов. Гипотез не осталось.

### Что Kilo считает неприменимым

- Ничего из основного assessment неприменимо не найдено. Review grounded и конкретный.

### Что внешний чат не увидел

- V2 request report не был прочитан (404). Внешний чат корректно использовал prompt content как review goal. Это дефект V2 process, а не контрактов.
- Compare link не был использован. Внешний чат честно указал `not read`. Для docs-only review это приемлемо.

## Proposed Implementation Plan

### Рекомендуемый следующий шаг

Один Kilo docs task на consistency cleanup, затем Codex review, затем acceptance.

### Предлагаемые изменения

1. **V2 prompt URL fix** — исправить central rules URL в V2 prompt template и в instantiated prompt.
2. **Journal path normalization** (Issue 3) — выбрать единый путь: `.ai/v3/journals/` (множественное число, как рекомендует внешний чат). Обновить `v3_journal_contract.md` и `v3_storage_policy.md`.
3. **"files outside files/" fix** (Issue 4) — уточнить формулировку в `v3_artifact_package_contract.md`: "Только package control files могут быть в корне ZIP: manifest.yaml, README_FOR_KILO.md, README_FOR_CODEX.md, checksums.sha256. Все project files — под `files/`."
4. **expected_files fix** (Issue 5) — уточнить в `v3_request_contract.md`: "expected_files — ожидаемые project-relative target paths. Внешний пакет кладёт их под `files/<project-relative-path>`."
5. **Verdict enum normalization** (Issue 6) — добавить нормализацию в `v3_codex_review_contract.md` и `v3_acceptance_policy.md`:

```yaml
codex_verdict:
  - accept
  - accept_with_notes
  - revision_needed
  - reject

human_verdict:
  - accept
  - revision
  - reject
```

6. **Scope examples** (Issue 7) — добавить примеры в `v3_scope_policy.md`:

```text
.ai/v3/contracts/*.md => workflow_docs
docs/v3/*.md => docs_only или workflow_docs в зависимости от purpose
JSON/YAML schemas => schemas
```

### Что НЕ делать

- Не пересоздавать контракты с нуля.
- Не добавлять Phase 3 prompts/templates — следующий шаг.
- Не создавать `scripts/v3/*`.
- Не активировать `/v3` shortcut.

### Human check needed?

`yes` — после реализации cleanup проверить, что контракты согласованы по терминологии.

## Статус V2 request после ingest

`implementation_planned`

## Баги и сложности (policy)

**Status: found and can be fixed in this run**

- Issue 1 (V2 request report URL) — process issue, не влияет на контракты.
- Issues 2-7 — все исправимы в рамках одного docs cleanup run.

## Human Check (policy)

Status: suggested

После реализации consistency cleanup:
1. Открыть 2-3 ключевых контракта (`v3_artifact_package_contract.md`, `v3_journal_contract.md`, `v3_acceptance_policy.md`).
2. Убедиться, что термины согласованы.
3. Если всё ок — подтвердить acceptance Phase 2.

## Следующий шаг после ingest

Реализовать proposed cleanup в рамках этого же запуска или отдельным Kilo docs task.
