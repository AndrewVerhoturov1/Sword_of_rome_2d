# V2 Ingest Summary: V2-20260606-231700

## Source

External senior review via GPT-5.5 Thinking. Response pasted directly into Kilo run.

## Verdict

Fix is a **real improvement** — self-certification closed for default no-upstream path. But truth model not fully closed.

## Key findings

1. **`upstream_evidence` boolean trust flag**: `upstream_evidence_available` is a caller-provided bool. No structured evidence check — caller can assert `true` without proof.
2. **Selected-step indexing bug**: UI sends `step_index` values; audit uses as array offsets → wrong step or silent drop.
3. **`_is_summary_basis_cumulative()` fragile**: substring matching on usage_basis — misses naming variants.
4. **Missing regression tests**: trust-boundary, indexing, basis marker gaps.

## Recommended next steps

1. Replace `upstream_evidence_available: bool` with structured evidence result
2. Fix selected-step mapping (align UI step_index with audit indexing)
3. Harden basis detection with explicit enum-like values
4. Add regression tests for trust-boundary and indexing

## Status

Findings noted. Fixes deferred to next correction slice (handoff 0050+).
